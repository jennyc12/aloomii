# SITE-IDEAS

Ideas parked here instead of the build. Nothing below is live copy.

## Homepage / studio system
- Animated "flywheel" diagram showing the four properties feeding each other
  (community → builders → podcast → doors → software → work).
- Per-property live stats band (DevKick member count, PBN cumulative reach)
  pulled from the same auto-updating sources palebluenexus.com uses.
- A short "How a referral becomes a build" explainer to reinforce the
  selective/by-referral positioning without adding intake CTAs.

## Chambers vertical
- When the chamber website ships this fall, replace the "Coming" placeholder
  receipt card with real metrics: member count, modules shipped (CMS, CRM,
  events, directory), and a link to a dedicated case-study page.
- Possible future: a /chambers landing page targeting other regional chambers
  (only after the first case study is real).

## The Table
- Rotating "rooms we own" calendar: upcoming DevKick nights and PBN tapings
  Table clients can actually attend (would need an events feed).

## Cross-linking (identities stay separate)
- devkick.com and palebluenexus.com should link back to aloomii.com as the
  studio behind them — text links only, each site keeps its own palette and
  wordmark. Never import DevKick's #CAFE00 or wordmark styling here.
